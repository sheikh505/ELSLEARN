<%@courses.each do |x| %>
    <% unless @user.present? %>
        <%= check_box_tag "course_#{@degree_id.to_s}[]", x.id,false %><span><%=x.name %></span>
    <% else %>
        <%= check_box_tag "course_#{@degree_id.to_s}[]", x.id,!@teacher_courses.nil? && @teacher_courses.any? { |b| b[:course_id] == x.id && b[:degree_id] == @degree_id } %><span><%=x.name %></span>
    <% end %>

<% end %>